services:
  - type: web
    name: legisaudit
    env: node
    nodeVersion: 20
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
