services:
  - type: web
    name: legisaudit
    env: node
    nodeVersion: 20
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
